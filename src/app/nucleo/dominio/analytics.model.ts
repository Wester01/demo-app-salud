export interface StatRapida {
  label: string;
  valor: string;
  tendencia: string;
  icono: string;
}

export interface ArticuloInfluyente {
  titulo: string;
  visitas: number;
  conversion: string;
}